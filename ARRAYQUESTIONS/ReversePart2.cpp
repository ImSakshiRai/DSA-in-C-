//reverse an array with given input after that we have to reverse it
#include<iostream>
#include<vector>
using namespace std;
/*
 void reverseArray(vector<int> &arr , int m){

     int s = m+1,e=arr.size()-1;
     while(s<=e){
       swap( arr[s],arr[e]);
           s++;
           e--;
     }   
 }
    void print(vector<int> arr) {

        for(int i=0; i<arr.size();i++ ) {
        cout<< arr[i] << " " ;
        }
        cout<<endl;
    }

int main(){
    vector<int> arr;

    arr.push_back(11);
    arr.push_back(5);
    arr.push_back(1);
    arr.push_back(4);
    arr.push_back(6);
    arr.push_back(8);
    arr.push_back(9);

    vector<int>  ans = reverseArray();
    cout<<"printing reverse array" <<endl;
    print (ans);
    return 0;
}
*/